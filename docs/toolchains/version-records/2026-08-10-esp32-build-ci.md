# Version Record: ESP32 firmware build（CI）

> 対象: [#42](https://github.com/wachi-yoshitaka-11-dev/deskcat/issues/42)
> 位置づけ: [2026-08-06 の開発端末での記録](2026-08-06-esp32-build-linux.md)を、**別環境で再現したかどうか**の記録

初回の検証は Linux x86_64 の開発端末 1 台で行った。本記録は、Pull Request #86 の tree を
GitHub Actions の `ubuntu-24.04` runner で clean build した結果である。

**3 回の実行は同一 commit ではない。**内訳は次のとおりで、**差分は文書 file だけである**
（`firmware/` 配下と workflow は 663a486 から変わっていない）。

| run | commit | cache | 結果 |
|---|---|---|---|
| 31359366171（初回） | 663a486 | 無し | success |
| 31359366171（再実行） | 663a486 | 有り | success |
| 31360185538 | 8889281 | 有り | success |

## 記録

```text
Record ID: 2026-08-10-esp32-build-ci
Date: 2026-08-10
最終有効な検証日時: 2026-08-10T06:01:41Z（run 31360185538）
Machine profile: CI（[Machine Profiles](../machine-profiles.md)）
Operator role: 自動実行（人間の介在なし）
Repository commit: 663a486（run 31359366171）／8889281（run 31360185538）。いずれも Pull Request #86 の head
Working tree clean: yes（checkout 直後。workflow は tree を変更しない）

OS name: Ubuntu
OS version: 24.04（runner image `ubuntu-24.04`、provisioner 20260707.563）
CPU architecture: x86_64
Userspace bitness: 64
Container / VM / native: VM（GitHub-hosted runner）

Rustup version: runner image 既定（記録せず。toolchain の同一性は下の名前で担保する）
Rust channel: esp-1.95.0.0（`rust-toolchain.toml` が要求する版付き名）
Rust compiler version: Xtensa Rust 1.95.0.0
Rust host: x86_64-unknown-linux-gnu
Installed Rust targets: xtensa-esp32-espidf
Cargo version: 上記 toolchain 同梱
rustfmt version: 同上
Clippy version: 同上
Linker identity and version: ldproxy（action の `ldproxy: true` で導入）

ESP32 only:
  Physical board: 接続なし（build-only。flash と serial monitor は実行しない）
  Module marking: 該当なし
  Board revision: 該当なし
  Rust target: xtensa-esp32-espidf
  espup version: esp-rs/xtensa-toolchain v1.7.0 が導入（commit ec6d365 へ固定）
  cargo-generate version: 使用せず
  ldproxy version: 上記 action が導入
  espflash version: 導入せず（flash を行わないため）
  ESP-IDF version: v5.5.3（`.cargo/config.toml` が `force = true` で固定）
  ESP-IDF source/commit: esp-idf-sys が取得。取得先は `.embuild`
  ESP-IDF tools location mode: workspace（`ESP_IDF_TOOLS_INSTALL_DIR`）
  IDF_PATH present: no
  IDF_TOOLS_PATH present: no
  Template repository: 該当なし（既存 tree を build）
  Template commit: 該当なし
  sdkconfig/defaults identity: 追跡下の `firmware/esp32/sdkconfig.defaults`
  USB-UART identity: 該当なし

Commands run:
  cargo fmt --all -- --check
  cargo clippy --all-targets --locked -- -D warnings
  cargo build --locked
Expected result: 3 command すべてが成功する
Actual result: 3 回の実行すべてで 3 command が成功（run 31359366171 を 2 回、run 31360185538 を 1 回）
Build duration: 下の「所要時間」を参照
Peak memory if measured: 未測定
Storage delta if measured: 未測定。cache 対象の合計は 327 MB（342,784,591 B）
Generated artifact identity: 未取得。**本記録は artifact の同一性を主張しない**（下記）
Log or evidence path: GitHub Actions run 31359366171（初回と cache 有りの再実行）、run 31360185538（後続 commit）
Known differences from documented profile:
  - 実機 Linux ではなく GitHub-hosted の VM である
  - board を接続していない。flash、serial monitor、実機起動は行っていない
  - `.embuild` を cache していないため、ESP-IDF は run ごとに取得している
Conclusion: Partial（build-only の command はすべて成功。未実行・未測定の項目が残るため下記）
Next action: required status check の必須化を別途判断する
未実行・未測定の項目:
  - peak memory と storage delta（template では任意項目）
  - flash、serial monitor、実機起動（CI profile の対象外。#6 の範囲）
  - 成果物の sha256（比較の基準にできないため取得しない。下記）
```

## 所要時間

3 回実行した。1 回目は cache 無し、2 回目と 3 回目は 1 回目が保存した cache を復元している
（いずれも `Cache hit ... on the primary key`）。

| step | cache 無し（1 回目） | cache 有り（2 回目） | cache 有り（3 回目） |
|---|---|---|---|
| Set up job | 1 s | 2 s | 1 s |
| Checkout | 0 s | 1 s | 1 s |
| Cache restore | 1 s（miss） | 8 s（hit） | 8 s（hit） |
| Install Xtensa Rust toolchain | 58 s | 53 s | 62 s |
| Format | 0 s | 0 s | 0 s |
| Lint | 263 s | 185 s | 219 s |
| Build | 19 s | 14 s | 20 s |
| Cache save | 5 s | 0 s | 0 s |
| **合計** | **349 s** | **264 s** | **311 s** |

cache 対象は `~/.cargo/registry`、`~/.cargo/git`、`firmware/esp32/target` の 3 つで、
復元 size は 327 MB（342,784,591 B）だった。

### この数字をどう読むか

cache 有りの 2 回でも **264 s と 311 s** と 47 s ずれている。`Lint` 単体で 185 s と 219 s である。
**cache による差（349 s に対して 38〜85 s）は、runner 側のばらつきと同じ桁にある。**

したがって **n = 3 では「cache で何 % 速くなる」と言える精度が無い。**
言えるのは次の 2 点だけである。

- cache 有りの 2 回は、いずれも cache 無しの 1 回より速かった
- 差は最大でも 85 s であり、**全体（約 5 分）を大きく変える規模ではない**

`.embuild` を cache しない構成では、**ESP-IDF の取得と build が毎 run 発生する。**
cache 有りでも `Lint` が 185〜219 s 掛かるのは主にこの分である。cache が効いているのは
crate の取得と再 compile であって、支配的な部分ではない。
所要時間を本気で縮めるなら、**削るべきは `.embuild` の再取得であって、この cache ではない。**
それは上限 10 GB との兼ね合いで採らなかった（次節）。

### `.embuild` を cache しない判断

`.cargo/config.toml` が `ESP_IDF_TOOLS_INSTALL_DIR = "workspace"` を固定しており、
ESP-IDF 本体と managed tool が `.embuild` へ入る。開発端末での実測は **4.4 GB** であり、
repository あたりの cache 上限 10 GB に対して大きい。単一 entry が上限の半分近くを占めると、
LRU eviction で他の cache を追い出す。

**この判断の代償は上の所要時間そのものである。**速度を取るなら `.embuild` を cache する側に
倒すことになるが、その場合は上限との兼ね合いを別途評価する必要がある。
現状は「毎 run 約 5 分」を受け入れて、cache 上限を圧迫しない側を選んでいる。

## artifact の同一性について

**本記録は成果物の sha256 を取得していない。**開発端末の記録が
「記録した入力を揃えても sha256 は一致しなかった（実測）」と結論しており、
環境が違う CI で一致を期待する根拠が無い。**取得しても比較の基準にできないため取らない。**

本記録が主張するのは、**同じ commit と同じ lockfile が、別環境で clean build を通ること**である。
`--locked` が成功していることは、`Cargo.lock` からの逸脱が無いことを示す。

## 「検証の移送」との対応

[Machine Profiles](../machine-profiles.md) の「検証の移送」が別端末へ求める項目を、本記録が満たすかを示す。

| 求められる項目 | 本記録 |
|---|---|
| OS と CPU architecture | Ubuntu 24.04 / x86_64 |
| container、VM、実機のどれか | **VM**（GitHub-hosted runner） |
| toolchain と target | Xtensa Rust 1.95.0.0（`esp-1.95.0.0`）/ `xtensa-esp32-espidf` |
| linker と SDK | ldproxy / ESP-IDF v5.5.3 |
| repository commit | 663a486 ／ 8889281（差分は文書のみ） |
| lockfile が変更されていないこと | `--locked` が成功 |
| clean build の結果 | 成功（fresh runner。`target` は cache 復元しうるが、初回は cache 無しで成功している） |

同文書は「ESP32 の build-only 検証」について **Docker 上の Linux を使ってよい**と定めており、
「container で得た結果を実機の根拠として扱わない」制限は flash と実機試験に掛かる。
本記録は build-only であり、flash と実機起動を主張しない。

## 制限

- **実機動作を一切主張しない。**flash、serial monitor、boot 確認は [#6](https://github.com/wachi-yoshitaka-11-dev/deskcat/issues/6) の範囲である
- runner image は GitHub が更新する。将来の run が同じ image とは限らない
- toolchain 名の一致は強制されるが、**その名前の中身が本当に 1.95.0.0 かは検査していない**（[#74](https://github.com/wachi-yoshitaka-11-dev/deskcat/issues/74) の残る穴と同じ）。CI では action の `version` 入力が版を決めている
- peak memory と storage delta は未測定である
