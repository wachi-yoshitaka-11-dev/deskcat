# 配置・運用

`deskcatd`が作成された後、Raspberry Pi用serviceとinstall用artifactをこのディレクトリに置く。

予定内容:

- `systemd` unit
- install・update手順
- logとrestartの方針
- 設定pathの説明

実行file、path、実行user、service動作が確定する前に、動作するdeployment scriptを作成しない。
